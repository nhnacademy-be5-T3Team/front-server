package com.t3t.frontserver.member.model.constant;

/**
 * 회원의 역할을 나타내는 enum <br>
 * 회원은 `USER(일반 사용자)`, `ADMIN(관리자)` 두 가지 역할을 가질 수 있다.
 * @author woody35545(구건모)
 */
public enum MemberRole {
    USER, ADMIN
}
